# Domain (localhost for local dev, your domain for production)
DOMAIN_NAME=localhost

# OpenAI API Key (required for LLM features)
OPENAI_API_KEY=

# Redis (default works with docker-compose)
REDIS_URL=redis://redis:6379
