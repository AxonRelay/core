fastapi
uvicorn[standard]
pydantic
python-dotenv
slowapi
sqlalchemy
alembic
psycopg2-binary
langgraph-sdk
# Pinned to the 2.x line: 2.0 renamed FastMCP -> MCPServer and moved it to
# mcp.server.mcpserver, so an unpinned install silently breaks app/mcp/server.py.
mcp>=2.1,<3
